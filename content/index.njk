---
layout: layouts/base.njk
---

{% set postslist = collections.posts %}
{% include "postslist.njk" %}

<a> <img src="/img/backgroundimage.jpeg" width="100%" height="50" alt="A"></a>

<h2>Blog Topics:</h2>

<ul>
<li>Started my new job this week - Apr 22nd</li>
<li>Attending the All Day Hey conference in Leeds - May 2nd</li>
<li>Attending the Agile Manchester Conference - May 15th to May 17th</li>
</ul>

<!-- Attempting to pull through my github repos in an iframe-->
    <div style="text-align: center;">
    <img src="https://github-readme-stats.vercel.app/api?username=ma1ab3nn" alt="Your Name's GitHub stats" style="border:1px solid black;" />
    </div>

    <h1>My GitHub Repositories</h1>
    <div id="repos"></div>

        <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            padding: 20px;
            background-color: #f4f4f4;
        }

        h1 {
            text-align: center;
        }

        .repo {
            background: white;
            margin: 10px auto;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            transition: transform 0.3s ease;
        }

        .repo:hover {
            transform: scale(1.05);
        }

        .repo h2 {
            margin: 0 0 10px;
            font-size: 1.5em;
        }

        .repo p {
            margin: 0 0 10px;
            color: #555;
        }

        .repo a {
            color: #0366d6;
            text-decoration: none;
        }

        .repo a:hover {
            text-decoration: underline;
        }

        .repo .meta {
            font-size: 0.9em;
            color: #666;
        }

        .repo .meta span {
            margin-right: 15px;
        }

        .repo .meta i {
            margin-right: 5px;
        }
    </style>


    <script>
        const username = 'ma1ab3nn';
        const url = `https://api.github.com/users/${username}/repos`;

        fetch(url)
        .then(response => response.json())
        .then(data => {
            const repoContainer = document.getElementById('repos');
            data.forEach(repo => {
                const repoDiv = document.createElement('div');
                repoDiv.classList.add('repo');
                repoDiv.innerHTML = `
                    <h2><a href="${repo.html_url}" target="_blank">${repo.name}</a></h2>
                    <p>${repo.description ? repo.description : 'No description available'}</p>
                    <div class="meta">
                        <span><i class="fas fa-calendar-alt"></i> Updated ${new Date(repo.updated_at).toLocaleDateString()}</span>
                    </div>
                `;
                repoContainer.appendChild(repoDiv);
            });
        })
        .catch(error => console.error('Error fetching repos:', error));
    </script>